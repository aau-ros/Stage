#include "swarm_ds.hh"

using namespace Stg;
using namespace std;

namespace swarm
{
    Ds::Ds()
    {
        // initialize member variables
        id = 0;
        pose = Pose();
        model = NULL;
    }
}
